<Window x:Class="BOZea.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:BOZea.Helpers"
        Title="BOZea - Maritime Marketplace"
        Height="768"
        Width="1024"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Icon="/Views/Assets/logo.ico">

    <Window.Resources>
        <helpers:ViewModelViewSelector x:Key="ViewSelector"/>
    </Window.Resources>

    <Grid>
        <ContentControl x:Name="MainContent"
                        Content="{Binding CurrentViewModel}"
                        ContentTemplateSelector="{StaticResource ViewSelector}"/>
    </Grid>
</Window>
