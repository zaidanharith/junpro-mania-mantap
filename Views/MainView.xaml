<Window x:Class="junpro_mania_mantap.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:junpro_mania_mantap.Helpers"
        Title="BOZea Apps"
        Height="450"
        Width="800">

    <Window.Resources>
        <helpers:ViewModelViewSelector x:Key="ViewSelector"/>
    </Window.Resources>

    <Grid>
        <ContentControl Content="{Binding CurrentViewModel}"
                        ContentTemplateSelector="{StaticResource ViewSelector}"/>
    </Grid>
</Window>
