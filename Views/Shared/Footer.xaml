<UserControl x:Class="BOZea.Views.Shared.Footer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:BOZea.ViewModels.Shared"
             x:Name="Root"
             FontFamily="Plus Jakarta Sans"
             Height="Auto"
             MinHeight="160">

    <UserControl.DataContext>
        <vm:FooterViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Views/Styles/MaterialOverrides.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border>
        <Border.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FF227DCD"/>
                <GradientStop Color="#FF113F67" Offset="0.8"/>
            </LinearGradientBrush>
        </Border.Background>
        <Grid Margin="60,30,60,30" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Left Section - Tagline -->
            <StackPanel Grid.Column="0" VerticalAlignment="Top">
                <TextBlock Text="Sail More, Spend Less!"
                           Foreground="White"
                           FontSize="24"
                           FontWeight="Bold"
                           Margin="0,0,0,4"/>
                <TextBlock Text="Find Your Need in One Place."
                           Foreground="White"
                           FontSize="24"
                           FontWeight="Bold"
                           Margin="0,0,0,12"/>
                <TextBlock Text="Shop all maritime related to make your works easy."
                           FontSize="16"
                           Foreground="White"/>
            </StackPanel>

            <!-- Right Section - Products Menu (Dynamic) -->
            <StackPanel Grid.Column="1" VerticalAlignment="Top" Margin="80,0,0,0" MinWidth="120">
                <TextBlock Text="Products"
                           Foreground="White"
                           FontWeight="Bold"
                           Margin="0,0,0,12"
                           FontSize="20"/>

                <!-- Dynamic Categories List -->
                <ItemsControl ItemsSource="{Binding Categories}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Margin="0,0,0,8" FontSize="16">
                                <Hyperlink Command="{Binding NavigateCommand}"
                                           Foreground="White"
                                           TextDecorations="None"
                                           Cursor="Hand">
                                    <Run Text="{Binding CategoryName}"/>
                                    <Hyperlink.Style>
                                        <Style TargetType="Hyperlink">
                                            <Setter Property="Foreground" Value="White"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Foreground" Value="#B3D9FF"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Hyperlink.Style>
                                </Hyperlink>
                            </TextBlock>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
